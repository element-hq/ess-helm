{{- /*
Copyright 2024 New Vector Ltd
Copyright 2025-2026 Element Creations Ltd

SPDX-License-Identifier: AGPL-3.0-only
*/ -}}

{{- with .Values.synapse -}}
{{- if .enabled -}}
{{- if not .media.storage.existingClaim }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    helm.sh/resource-policy: {{ .media.storage.resourcePolicy | default $.Values.storage.resourcePolicy }}
  labels:
    {{- include "element-io.synapse.labels" (dict "root" $ "context" .) | nindent 4 }}
  name: {{ $.Release.Name }}-synapse-media
  namespace: {{ $.Release.Namespace }}
spec:
{{- with coalesce .media.storage.storageClassName $.Values.storage.storageClassName }}
  storageClassName: {{ . }}
{{- end }}
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .media.storage.size }}
{{- end }}
{{- end -}}
{{- end -}}
