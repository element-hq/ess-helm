# Copyright 2025-2026 Element Creations Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only

# source_fragments: init-secrets-minimal.yaml postgres-minimal.yaml server-name.yaml synapse-minimal.yaml synapse-redis-secrets-in-helm.yaml synapse-redis.yaml synapse-some-workers-running.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

# initSecrets, postgres don't have any required properties to be set and defaults to enabled
deploymentMarkers:
  enabled: false
elementAdmin:
  enabled: false
elementWeb:
  enabled: false
matrixAuthenticationService:
  enabled: false
matrixRTC:
  enabled: false
serverName: ess.localhost
synapse:
  ingress:
    host: synapse.ess.localhost
  redis:
    host: external-redis.example.com
    password:
      value: test-redis-password
    port: 6379
  workers:
    appservice:
      enabled: true
    client-reader:
      enabled: true
    event-persister:
      enabled: true
    federation-reader:
      enabled: true
wellKnownDelegation:
  enabled: false
