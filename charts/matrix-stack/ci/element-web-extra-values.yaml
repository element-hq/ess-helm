# Copyright 2025 Element Creations Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only

# source_fragments: element-web-extra-volumes.yaml element-web-minimal.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

# initSecrets, postgres don't have any required properties to be set and defaults to enabled
deploymentMarkers:
  enabled: false
elementAdmin:
  enabled: false
elementWeb:
  extraVolumeMounts:
    - mountPath: /app/privacy_policy/en
      name: test-policydocs
      readOnly: true
  extraVolumes:
    - configMap:
        name: '{{ $.Release.Name }}-element-policydocs'
      name: test-policydocs
  ingress:
    host: element.ess.localhost
matrixAuthenticationService:
  enabled: false
matrixRTC:
  enabled: false
synapse:
  enabled: false
wellKnownDelegation:
  enabled: false
