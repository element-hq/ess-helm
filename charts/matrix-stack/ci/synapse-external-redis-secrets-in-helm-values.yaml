# Copyright 2025 Element Creations Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only

# source_fragments: init-secrets-minimal.yaml postgres-minimal.yaml server-name.yaml synapse-external-redis-in-helm.yaml synapse-minimal.yaml synapse-some-workers-running.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

# initSecrets, postgres don't have any required properties to be set and defaults to enabled
deploymentMarkers:
  enabled: false
elementAdmin:
  enabled: false
elementWeb:
  enabled: false
hookshot:
  enabled: false
matrixAuthenticationService:
  enabled: false
matrixRTC:
  enabled: false
serverName: ess.localhost
synapse:
  externalRedis:
    host: "external-redis.example.com"
    port: 6379
    password:
      value: "test-redis-password"
  ingress:
    host: synapse.ess.localhost
  workers:
    client-reader:
      enabled: true
wellKnownDelegation:
  enabled: false
