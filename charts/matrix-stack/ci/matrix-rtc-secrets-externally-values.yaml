# Copyright 2024-2025 New Vector Ltd
# Copyright 2025-2026 Element Creations Ltd
#
# SPDX-License-Identifier: AGPL-3.0-only

# source_fragments: init-secrets-disabled.yaml matrix-rtc-additional-secrets-externally.yaml matrix-rtc-minimal.yaml matrix-rtc-redis-valkey-secrets-externally.yaml matrix-rtc-redis-valkey.yaml matrix-rtc-secrets-externally.yaml
# DO NOT EDIT DIRECTLY. Edit the fragment files to add / modify / remove values

# postgres don't have any required properties to be set and defaults to enabled
deploymentMarkers:
  enabled: false
elementAdmin:
  enabled: false
elementWeb:
  enabled: false
initSecrets:
  enabled: false
matrixAuthenticationService:
  enabled: false
matrixRTC:
  ingress:
    host: mrtc.ess.localhost
  livekitAuth:
    key: CHANGEME-oolahd9xooshohSh5IeQu1natheur1oo
    secret:
      secret: "{{ $.Release.Name }}-matrix-rtc-external"
      secretKey: livekitSecret
  # The authoriser doesn't yet support an external Redis / Valkey
  sfu:
    additional:
      example-value:
        configSecret: "{{ $.Release.Name }}-mrtc-external"
        configSecretKey: config
    redisOrValkey:
      host: "external-redis-or-valkey.example.com"
      password:
        secret: "{{ $.Release.Name }}-redis-valkey-credentials"
        secretKey: "password"
      port: 6379
synapse:
  enabled: false
wellKnownDelegation:
  enabled: false
