Allow the `syn2mas` migration process to stay in stage 2 for multiple deployments.

The actual `syn2mas` Helm hooks will keep running but will not doing anything until `matrixAuthenticationService.syn2mas.enabled: false` and the migration is finalised.
