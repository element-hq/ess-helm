Pin pytest to `<9` in the `ess-community-integration-tests` package as well, so downstream consumers that `uv tool install` the test package (rather than running from the locked workspace) don't pull pytest 9, which breaks `pytest-asyncio-cooperative`. Completes the pin from #1301.
