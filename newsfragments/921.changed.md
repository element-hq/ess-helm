Support configuring IPv4-only, IPv6-only or Dual-Stack component binds by configuring `networking.ipFamily`.

Defaults to `dual-stack` but if you are in a cluster where IPv6 support is either disabled at boot or compiled out you may need to set this to `ipv4`
