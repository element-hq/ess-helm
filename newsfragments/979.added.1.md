Add support for Hookshot installation in the chart.

[Hookshot](https://matrix-org.github.io/matrix-hookshot/latest/hookshot.html) is a Matrix Bot for connecting to external services.
It is not enabled by default, but can be enabled by setting `hookshot.enabled: true`.
