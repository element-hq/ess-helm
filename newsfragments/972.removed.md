Move Synapse's Redis to a top-level shared component that can be used by multiple components of the chart.

There is no impact when using the default values, but if you have customised values under the `synapse.redis` key, you will need to update them to be under the new top-level `redis` redis.
