redis: move synapse-redis to a shared component that can be used by multiple components of the chart. There is no impact when using default values, but if you have customize `synapse.redis` values, you will need to update them to use `redis`.
