Remove `/tmp` emptyDir from Synapse pods. If your modules or configuration need a read-write access to /tmp, you should now use `synapse.extraVolumes` and `synapse.extraVolumeMounts` to add this mount point.
