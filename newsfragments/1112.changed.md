Matrix Authentication Service: Remove hardcoded `kid` from secret keys in the config file, and let Matrix Authentication Service automatically derive them.

Matrix Authentication Service uses the `kid` to sign `id_tokens`. In the Matrix specification, clients are not supposed to verify the `id_token`.
This means it is fine to let Matrix Authentication Service manage the `kid` values at runtime.
