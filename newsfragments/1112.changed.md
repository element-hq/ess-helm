Matrix Authentication Service: Remove hardcoded kids from the config file, and let MAS automatically derive them from their values.

Matrix Authentication Service uses the kids to sign `id_tokens`. In Matrix specs, Clients are not supposed to verify the `id_token`.
This means it is fine to let Matrix Authentication Service manage the kids values at runtime.
