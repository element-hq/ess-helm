Better handle the only worker-capable delayed-events endpoint.

`^/_matrix/client/unstable/org.matrix.msc4140/delayed_events$` only handles `GET`
so this is clearer rather than relying on `path_map_file_get.tpl`. This is a slight
divergence from the docs for now but should be addressed soon.
