Add support for configuring `affinity` (nodeAffinity, podAffinity and podAntiAffinity) on component workloads, along with a global `affinity` applied to all components. Each affinity type is resolved independently: a component that sets one type overrides only that type, inheriting the others from the global affinity, and a type can be blanked out per-component by setting it to `{}`.
