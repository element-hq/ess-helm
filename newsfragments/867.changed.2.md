Remove hard-coded `podAntiAffinity` for `Deployments` that had set `replicas > 2`.

We're now setting a configurable, soft `topologySpreadConstraint` that spreads multiple
replicas of the same component across nodes.
